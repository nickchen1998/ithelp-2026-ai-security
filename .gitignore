# 密鑰與環境變數
.env
.env.*

# 本機 Claude 設定
.claude/settings.local.json

# macOS
.DS_Store
**/.DS_Store

# 蒐集的條文原文（受著作權，勿推上公開 repo）
# 預設不追蹤 條文/ 下的 PDF，避免誤將 ISO/CNS 等受版權標準公開。
# 若某份確定可散布（如 OWASP CC BY-SA、法律條文），可用 git add -f 個別強制加入。
條文/*.pdf
