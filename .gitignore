# 密鑰與環境變數
.env
.env.*

# 本機 Claude 設定
.claude/settings.local.json

# macOS
.DS_Store
**/.DS_Store

# 條文/ 蒐集的參考條文（本 repo 為 private，故正常追蹤）。
# ⚠️ 提醒：ISO/CNS 等單人授權標準，即使 private repo 也屬「儲存／傳輸副本」，
#    與其授權條款有牴觸；若日後蒐集到 ISO/CNS 正本，建議留本機、勿加入版控。
