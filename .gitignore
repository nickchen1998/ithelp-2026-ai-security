# 密鑰與環境變數
.env
.env.*

# 本機 Claude 設定
.claude/settings.local.json

# macOS
.DS_Store
**/.DS_Store

# 條文/ 蒐集的參考條文一律排除於版控之外，僅保留本機。
# ISO/CNS 等單人授權標準即使在 private repo 也屬「儲存／傳輸副本」，與其授權條款牴觸；
# 已蒐集 ISO/IEC 42001 正本，故整個目錄留本機、不加入版控。
條文/
